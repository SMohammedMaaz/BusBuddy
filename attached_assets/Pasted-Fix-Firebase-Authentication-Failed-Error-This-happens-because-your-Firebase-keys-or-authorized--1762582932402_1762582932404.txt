Fix — Firebase Authentication “Failed” Error

This happens because your Firebase keys or authorized domains aren’t properly configured.

🔧 Solution:

Go to Firebase Console → Authentication → Settings

Under Authorized Domains, click “Add Domain” → add

replit.com
*.repl.co
localhost


Then, ensure your environment variables in Replit are set properly:

VITE_FIREBASE_API_KEY = your_key
VITE_FIREBASE_AUTH_DOMAIN = yourapp.firebaseapp.com
VITE_FIREBASE_PROJECT_ID = your_project_id
VITE_FIREBASE_APP_ID = your_app_id


Make sure Firebase SDK is initialized like this:

import { initializeApp } from "firebase/app";
import { getAuth, GoogleAuthProvider } from "firebase/auth";
const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY,
  authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN,
  projectId: import.meta.env.VITE_FIREBASE_PROJECT_ID,
  appId: import.meta.env.VITE_FIREBASE_APP_ID,
};
const app = initializeApp(firebaseConfig);
export const auth = getAuth(app);
export const provider = new GoogleAuthProvider();


That’ll fix your Firebase login and make both Google & OTP sign-ins work 💫

🎨 2️⃣ Interactive “Pop-Out” Dashboard Feature Cards

This will make your UI feel alive and interactive, just like you imagined 💫

✅ Add with Framer Motion:
import { motion } from "framer-motion";

const FeatureCard = ({ title, description, icon }) => (
  <motion.div
    className="bg-white/10 backdrop-blur-md rounded-2xl p-6 text-center shadow-lg cursor-pointer"
    whileHover={{ scale: 1.1, boxShadow: "0px 10px 30px rgba(0,166,150,0.5)" }}
    whileTap={{ scale: 0.95 }}
    transition={{ type: "spring", stiffness: 300 }}
  >
    <div className="text-5xl mb-4">{icon}</div>
    <h3 className="text-xl font-semibold text-white mb-2">{title}</h3>
    <p className="text-white/80">{description}</p>
  </motion.div>
);


This makes each feature card pop out when clicked and gently animate back when deselected.

🌱 3️⃣ New Eco Documentation Feature — Bus Fitness & Pollution Compliance Tracker
💡 Problem:

Many city buses run on expired pollution or fitness certificates, causing major emissions.

💚 Solution:

Add a “Bus Documentation Validation” section to your admin dashboard:

📂 Fields:

Vehicle Number

Pollution Certificate (PDF upload + expiry date)

Fitness Certificate (PDF upload + expiry date)

Compliance Status → ✅ Valid / ⚠️ Expiring Soon / ❌ Expired

🧠 AI/Automation:

Automatically alert admin when certificate is about to expire (within 15 days).

Flag vehicles that are “Unfit” for eco-standards → suggest replacement or servicing.

Integration with pollution control databases (future expansion).

UI Example:
<motion.div className="bg-white/10 rounded-2xl p-6 text-center hover:scale-105 transition">
  <h3 className="text-xl font-bold text-green-400">Vehicle Fitness Status</h3>
  <p className="text-white/70 mt-2">Bus #KA09-9485</p>
  <p className="text-sm mt-1">Pollution Cert: <span className="text-green-300">Valid till Dec 2025</span></p>
  <p className="text-sm">Fitness Cert: <span className="text-red-400">Expired</span></p>
  <button className="mt-3 px-4 py-2 bg-green-500 text-white rounded-lg hover:bg-green-600">Renew / Notify Admin</button>
</motion.div>

📍 4️⃣ Smart Bus Proximity Messaging (for commuters)

Feature:
If a user pins a bus (say Route 119A) and that bus comes within 1 km radius, send a notification or WhatsApp message.

⚙️ Implementation:

Store user’s selected bus + current location in Firestore.

Continuously check bus GPS location (Firebase Realtime DB updates).

If distance(user_location, bus_location) <= 1km → Trigger:

Firebase Cloud Messaging (Push Notification)

Twilio WhatsApp/SMS Alert

Sample Message:

“🚍 Your pinned bus #119A is just 1 km away! ETA: 3 mins 🌱 Get ready to board and travel green!”

🧩 5️⃣ Updated Dashboard Sections (After Login)
Section	Purpose	Animations
🗺️ Live Map	Google Maps API + moving markers	Smooth bus marker motion
📊 Analytics	CO₂ saved, route efficiency	Gradient color bar animations
📁 Documentation	Upload & verify vehicle certificates	Slide-in panel
🔔 Alerts	Bus proximity, doc expiry, eco tips	Slide up notification tray
💬 Chat/Group	Passenger coordination / eco campaigns	Message fade animations
🎨 6️⃣ Enhanced Color Palette (Professional + Friendly)
Element	Color	Description
Background Gradient	linear-gradient(135deg, #003049, #00A896, #0072BB)	Smooth eco feel
Card Glow	rgba(0, 168, 150, 0.5)	Green luminescence
Button Gradient	from-[#00A896] to-[#004E92]	Bright eco blue-green
Icon Accent	#B2FF59	Lime for freshness
Text	#F8FAFC	Clean white for readability
🚦 7️⃣ Bonus Enhancements

🌍 Offline Tracking → Use IndexedDB to cache bus ETA and route data.

🧠 AI Prediction Tooltip → “Traffic ahead may delay ETA by 4 mins.”

🌤️ Weather-Aware ETA → Adjusts travel time in rain.

🏅 Eco Score → Award eco-points for frequent bus usage.

✅ In Short — Your Enhanced BusBuddy 2.0 Includes:

🌱 Animated interactive dashboard cards (pop-out effect)

🔐 Fixed Firebase login (Google + OTP)

🧾 Bus documentation verification (pollution + fitness)

📍 Smart proximity alerts (1km notification system)

🎨 Aesthetic eco dashboard with glow & motion

🌤️ Optional weather + CO₂ AI analytics